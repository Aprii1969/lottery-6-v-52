

> **Пройти весь диапазон 1000–2527**,
> и **заполнить по каждому тиражу все ключевые метрики**,
> затем **сохранить в единую структуру ("объединённая матрица")**

📌 Это не просто "как вариант" — это отличный фундамент
для Observer, обучения, визуализации, настройки.

---

## ✅ Что будет входить в **объединённую матрицу по тиражам**

| Поле               | Что содержит                                          |
| ------------------ | ----------------------------------------------------- |
| `structure_LMH`    | Распределение по линейным зонам (например: 1-2-3)     |
| `structure_HMC`    | Распределение по частотным зонам (Hot/Mid/Cold)       |
                       H/M/C → на основе частотности только до тиража
| `glue_match`       | Есть ли активные glue/anchor связи                    |
| `sleepy_hits`      | Сколько номеров были sleepy (до тиража)               |
| `repeated_numbers` | Повторы с последними 5–10 тиражами                    |
| `parity_profile`   | Чёт/нечёт / сбалансированность                        |
| `f10_f20_avg`      | Средняя частота номеров до тиража                     |
| `zone_breakdown`   | Карта: какой номер в какой зоне (на момент ДО тиража) |
| `structure_repeat` | Совпадала ли структура с одной из предыдущих N        |

📁 Сохраняется в `draw_structures_matrix.json` или `.csv` — для просмотра, анализа и использования.

---

## 🔧 Как реализуем (по шагам):

### 1. `for draw in draws[1000:2527]`:

* взять номера тиража
* получить историю до него

### 2. Рассчитать:

* L/M/H → деление по 1–17, 18–34, 35–52
* H/M/C → на основе частотности до тиража
* Sleepy → номера не появлялись ≥ X тиражей
* Glue → есть ли связи между выпавшими номерами
* Повторы → были ли в последних 10-тиражах
* Parity → сколько чёт/нечёт
* Квота → по зонам
* Форма структуры → “2-2-2” и пр.

---

## ✅ Почему это лучший формат:

| Преимущество                    | Польза                                 |
| ------------------------------- | -------------------------------------- |
| Централизованно, честно, полно  | Не надо пересчитывать в разных модулях |
| Основа для обучения моделей     | Форма → попадание → поведение          |
| Подходит для Observer и анализа | Поиск паттернов, циклов, исключений    |
| Поддерживает Blind Test         | Каждое значение рассчитывается до `n`  |



## 📌 Вывод:

> **Да, делаем именно так:**
> 📊 Проходим 1000–2527,
> 🧠 Заполняем все метрики,
> 💾 Формируем `draw_structures_matrix.json`
> 📡 LABCORE и Observer используют её как знание и контекст.

